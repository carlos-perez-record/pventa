<div id="technology-modal" class="modal fade"> <!-- product-modal -->
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title"><%= title %></h4>
      </div>
      <%= form_for @t, remote: true do |f| %>
        <div class="modal-body">
          <div class="form-group">
            <%= f.text_field :descripcion, class: "form-control input-lg", placeholder: "Ingresar Nueva Tecnologia" %>
          </div>
          <div class="form-group">
            <%= f.text_field :serial, class: "form-control input-lg", placeholder: "Ingrese el serial" %>
          </div>
          <div class="form-group">
            <%= f.text_field :celda, class: "form-control input-lg", placeholder: "Ingrese la celda asociada" %>
          </div>
        </div>

        <div class="modal-footer">
          <%= f.submit "Registrar", class: "btn btn-danger btn-lg btn-block" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
