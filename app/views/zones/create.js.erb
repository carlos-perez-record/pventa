<% if @z.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody').append(' <%= j render "row", zone: @z %>');
  $('.alert-success').html("La zona ha sido creada").show();
  $('#center-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @z.errors.full_messages[0] %>');
<% end %>
